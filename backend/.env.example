# 知乎开放平台密钥。请复制为 .env 后填写，不要提交真实密钥。
ZHIHU_ACCESS_SECRET=

# OpenAI-compatible API。Base URL 示例通常以 /v1 结尾，按服务商文档填写。
OPENAI_API_KEY=
OPENAI_BASE_URL=
MODEL_NAME=deepseek-flash

# 允许访问后端的前端地址，多个地址使用英文逗号分隔。
# 本地默认地址已由代码包含；部署后填写 Vercel 的 HTTPS 地址。
FRONTEND_ORIGINS=
